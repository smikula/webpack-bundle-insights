import React from 'react';
import { StatsPicker } from '../components/StatsPicker';
import './App.css';

export const App: React.FC<{}> = () => {
    return (
        <div>
            <StatsPicker />
        </div>
    );
};
